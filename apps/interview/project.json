{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "interview",
  "projectType": "application",
  "sourceRoot": "apps/interview/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/interview"],
      "options": {
        "command": "tsc -p apps/interview/tsconfig.json && vite build --config apps/interview/vite.config.ts"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite --config apps/interview/vite.config.ts"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite preview --config apps/interview/vite.config.ts"
      }
    }
  }
}
