#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

if ! "$ROOT/scripts/sync-portfolio.sh"; then
  echo "Stylophone was pushed, but the Portfolio sync failed." >&2
  exit 1
fi
