#!/usr/bin/env bash
# ─────────────────────────────────────────────────────────────
#  pre-commit hook — CLAUDE.md freshness check
#
#  If any portfolio source file is staged, CLAUDE.md must also be staged.
#  Bypass:  git commit --no-verify
# ─────────────────────────────────────────────────────────────

STAGED=$(git diff --cached --name-only)

# No portfolio source files staged → nothing to enforce
if ! echo "$STAGED" | grep -q '^apps/portfolio/src/'; then
  exit 0
fi

# Portfolio source files staged → require CLAUDE.md to be staged too
if echo "$STAGED" | grep -q '^CLAUDE\.md$'; then
  exit 0
fi

echo ""
echo "┌─────────────────────────────────────────────────────────┐"
echo "│  ✗  CLAUDE.md is stale                                  │"
echo "│                                                          │"
echo "│  You staged portfolio source changes but CLAUDE.md was  │"
echo "│  updated. Document what changed, then:                   │"
echo "│                                                          │"
echo "│    git add CLAUDE.md                                     │"
echo "│                                                          │"
echo "│  To skip this check:                                     │"
echo "│    git commit --no-verify                                │"
echo "└─────────────────────────────────────────────────────────┘"
echo ""

exit 1
